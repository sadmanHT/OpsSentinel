global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: opssentinel-backend
    metrics_path: /observability/metrics
    static_configs:
      - targets:
          - backend:8000
